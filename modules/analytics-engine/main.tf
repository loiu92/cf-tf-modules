/* wrangler-owned — Analytics Engine has no Terraform resource in provider 5.x.
   Access via the `analytics_engine_datasets` wrangler binding. cf-bootstrap
   emits a comment-only TF placeholder and renders the binding when selected.
   Tracking: Analytics Engine (analytics_engine binding). */
