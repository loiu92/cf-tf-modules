/* NOT YET SUPPORTED by the cloudflare terraform provider (~> 5.0) — Durable
   Object namespaces are created implicitly via Worker script migrations
   (cloudflare_workers_script `migrations` block) or the dashboard/API. No
   standalone `cloudflare_durable_object_namespace` resource exists as of 5.x.
   Tracking: Durable Objects (cloudflare_durable_object_namespace). */
