/* wrangler-owned — Cloudflare Images has no discrete Terraform resource for the
   Worker binding in provider 5.x. cf-bootstrap emits a comment-only TF
   placeholder and renders the `images` binding when this module is selected.
   Tracking: Images (images binding / Images Write permission). */
